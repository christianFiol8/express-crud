info:
  name: GET all users
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/users"
  headers:
    - name: auth
      value: "{{Auth}}"
  params:
    - name: ""
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
