info:
  name: UPDATE user
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseUrl}}/api/users/1"
  headers:
    - name: auth
      value: "{{Auth}}"
    - name: token
      value: "{{token}}"
  params:
    - name: ""
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "name": "Alice Actualizada",
        "birthdate": "1995-04-23"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
