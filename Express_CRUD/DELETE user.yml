info:
  name: DELETE user
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/users/3"
  headers:
    - name: auth
      value: "{{Auth}}"
    - name: token
      value: "{{token}}"
  params:
    - name: ""
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
